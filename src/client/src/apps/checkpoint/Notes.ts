import byField from './utils/byField';

const invitations: Note[] = [
	{
		title: 'snow_cleaner_title',
		body: 'snow_cleaner_body'
	},
	{
		title: 'random_title',
		body: 'random_body'
	},
];

// export const sortedByLastCall = [...contacts].sort(byField('lastCall'));
export default invitations;